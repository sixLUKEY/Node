{
  "name": "server",
  "version": "1.0.0",
  "description": "test server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sixlukey",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
